import { FiltroMayusculaPipe } from './filtro-mayuscula.pipe';

describe('FiltroMayusculaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroMayusculaPipe();
    expect(pipe).toBeTruthy();
  });
});
